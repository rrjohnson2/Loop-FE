import { IdeaFilterPipe } from './idea-filter.pipe';

describe('IdeaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdeaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
